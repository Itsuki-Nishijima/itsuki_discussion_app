.container
  .header
    .header-left
      %p.header-title Boards
      = image_tag "default-avatar.png", class: "header-avatar"

    .header-actions
      - if user_signed_in?
        = button_to "Log out",
          destroy_user_session_path,
          method: :delete,
          class: "logout-button"

  .content
    %h2.page-title All Boards

    - @boards.each do |board|
      .board-card
        .board-card-header
          %h3.board-title= board.title

          .board-menu
            …

        %p.board-description= board.description

        .board-card-footer
          .board-user-info
            = image_tag "default-avatar.png", class: "board-avatar"
            %span.board-user= board.user.email

    = link_to new_board_path, class: "new-board" do
      + Add new board